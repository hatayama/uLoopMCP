Comments in the code, commit messages, and PR titles and bodies should be written in English.

Always use `io.github.hatayama.uLoopMCP` as the namespace.

Always commit the `dist/` directory files (e.g., `Packages/src/TypeScriptServer~/dist/`) along with source changes. These bundled files are part of the repository.
