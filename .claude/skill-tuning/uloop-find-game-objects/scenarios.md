# Scenarios — uloop-find-game-objects

| ID | Type | Prompt |
|----|------|--------|
| M | median | "Player という名前の GameObject に Rigidbody が付いているか確認したい" |

closed-book mode. Median scenario probes name search + component filter + per-result field readout — exercises both the parameter table and the response shape (the primary self-containment hole).
