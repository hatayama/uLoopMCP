# Checklist — uloop-find-game-objects

## Scenario M

- `[critical]` Issue `uloop find-game-objects --name-pattern "Player" [--required-components Rigidbody]`
- `[critical]` State the per-GameObject field names (name, path, isActive, tag, layer, components)
- `[critical]` Identify which component field encodes the type (`type` short name, `fullTypeName` qualified)
- `[critical]` Recognize the multi-Selected file-export branch
- `[critical]` Self-report any unclear points or discretionary fill-ins
