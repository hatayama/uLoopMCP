# Checklist — uloop-screenshot

## Scenario M

- `[critical]` Issue `uloop screenshot --capture-mode rendering --annotate-elements` (or `--elements-only` variant)
- `[critical]` Identify `CoordinateSystem` field as the coordinate-convention indicator
- `[critical]` Recognize PlayMode requirement for `--capture-mode rendering`
- `[critical]` Identify `SimX`/`SimY` (or equivalent) as the field to feed into a mouse-input command
- `[critical]` Self-report any unclear points or discretionary fill-ins
