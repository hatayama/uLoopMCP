# Scenarios — uloop-screenshot

| ID | Type | Prompt |
|----|------|--------|
| M | median | "Game View をスクリーンショットして、画面内のボタン座標を取得して click したい" |

closed-book mode. Median scenario probes the canonical screenshot-then-mouse workflow (the highest-value When item from the description). Single scenario sufficient — other modes (Scene View capture, multi-window capture) share the same response contract.
