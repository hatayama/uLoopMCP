# Scenarios — uloop-run-tests

| ID | Type | Prompt |
|----|------|--------|
| M | median | "EditMode と PlayMode の両方のテストを走らせて、失敗があれば詳細を見たい" |

closed-book mode. Median scenario probes (a) parallel-safety constraint reading, (b) failure-detail inspection workflow (XmlPath → Read), (c) both-mode coverage. Single scenario sufficient — `--filter-type` modes share the same response shape.
