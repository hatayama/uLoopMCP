# Failure pattern ledger — uloop-launch

Per-target ledger. Patterns from other skills (e.g., uloop-compile, uloop-screenshot) must NOT be merged here.

Format: each entry has Pattern name, Example issue, General Fix Rule, Seen in (iter list), Fix landed in.

## Patterns

(no `uloop-launch`-local patterns. The single gap addressed in Iter 0 was a cross-skill carryover from `uloop-compile`'s `environment-leak self-containment gap`. See `.claude/skill-tuning/uloop-compile/ledger.md` for the canonical entry.)
